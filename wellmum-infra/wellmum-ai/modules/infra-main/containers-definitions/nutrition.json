[
    {
        "cpu": 0,
        "essential": true,
        "image": "${IMAGE}",
        "logConfiguration": {
            "logDriver": "awslogs",
            "options": {
                "awslogs-group": "/ecs/${ENVIRONMENT}-${PROJECT_NAME}-nutrition",
                "awslogs-create-group": "true",
                "awslogs-region": "${AWS_REGION}",
                "awslogs-stream-prefix": "ecs"
            }
        },
        "healthCheck": {
            "command": ["CMD-SHELL", "curl -f http://localhost:8001/healthz || exit 1"],
            "interval": 300,
            "timeout": 5,
            "retries": 3,
            "startPeriod": 60
        },
        "mountPoints": [],
        "name": "nutrition",
        "portMappings": [
            {
                "containerPort": 8001,
                "protocol": "tcp"
            }
        ],
        "systemControls": [],
        "volumesFrom": []
    }
]