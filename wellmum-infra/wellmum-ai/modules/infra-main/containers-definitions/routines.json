[
    {
        "cpu": 0,
        "essential": true,
        "image": "${IMAGE}",
        "logConfiguration": {
            "logDriver": "awslogs",
            "options": {
                "awslogs-group": "/ecs/${ENVIRONMENT}-${PROJECT_NAME}-routines",
                "awslogs-create-group": "true",
                "awslogs-region": "${AWS_REGION}",
                "awslogs-stream-prefix": "ecs"
            }
        },
        "healthCheck": {
            "command": ["CMD-SHELL", "curl -f http://localhost:8000/healthz || exit 1"],
            "interval": 300,
            "timeout": 5,
            "retries": 3,
            "startPeriod": 60
        },
        "mountPoints": [],
        "name": "routines",
        "portMappings": [
            {
                "containerPort": 8000,
                "protocol": "tcp"
            }
        ],
        "systemControls": [],
        "volumesFrom": []
    }
]