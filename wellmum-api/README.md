# Whelmom API Clone

Minimal Nest.js clone exposing a single healthcheck endpoint and Swagger UI.

How to run (Docker):

```bash
docker-compose up --build
```

Endpoints:
- GET /api/healthcheck
- Swagger UI: /api/docs
